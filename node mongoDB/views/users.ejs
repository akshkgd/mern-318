<%- include('nav') %>
    <div class="container">
        <div class="row justify-content-center mt-5">

            <div class="col-md-7">
                <% if(users.length == 0){ %>
                    <h3 class="text-center mt-5">No user found! 😔</h3>
                    <%}else{%>
                        <h3>All Registered Users</h3>
                        <p class="small">We never share your details 😎 Enter confidiently!</p>
                       
                        <%}%>
                       
                        <% for(let i=0; i< users.length; i++){ %>
                                <div class="card mb-3">
                                    <div class="card-body">
                                        <div class="d-flex  justify-content-between" style="align-items: center;">
                                            <div class="d-flex " style="gap:20px">
                                                <p class="m-0"><%= users[i].firstName %></p> <p class="m-0"><%= users[i].email %></p>
                                            </div>
                                            <div class="d-flex" style="gap:10px">
                                                <form action="/edit" method="post"><input type="hidden" name="email" value="<%= users[i].email %>"><button class="btn btn-outline-primary" type="submit">Edit</button></form>

                                                <form action="/delete" method="post"><input type="hidden" name="email" value="<%= users[i].email %>"><button class="btn btn-outline-danger" type="submit">Delete</button></form>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            <%}%>


                            
                    </div>
                </div>
            </div>
        
</body>

</html>